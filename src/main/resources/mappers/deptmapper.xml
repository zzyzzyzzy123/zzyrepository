<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="online.zzy.springbootmybatis.dao.DeptMapper" >
    <select id="selectDeptById" resultType="string">
        SELECT departmentname FROM dept WHERE id=#{id}
    </select>
</mapper>
